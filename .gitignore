# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# uv
# (uv.lock is committed; nothing to ignore)

# Env / secrets
.env
.env.local

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# ML / experiments
mlruns/
mlartifacts/
*.ckpt
*.pt

# Data (versioned via DVC later, not git)
/data/
*.csv.dvc.tmp

# Disposable MLflow-run-id eval report dirs (32-char hex). These are throwaway by
# default; publishing one is a deliberate act: `git add -f <dir>` + point LATEST/
# LATEST_EVAL at it in the same commit (see common/artifact_pointers.py).
artifacts/models/*/????????????????????????????????/

# Node / Next.js (Phase 3)
node_modules/
.next/
out/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
