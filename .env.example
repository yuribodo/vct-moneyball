# Copy to .env and adjust as needed.

# --- Postgres (consumed by docker-compose) ---
POSTGRES_USER=vct
POSTGRES_PASSWORD=vct
POSTGRES_DB=vct_moneyball
POSTGRES_PORT=5432

# --- Application ---
DATABASE_URL=postgresql+psycopg://vct:vct@localhost:5432/vct_moneyball
